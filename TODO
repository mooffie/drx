
== GUI

* 'drxtk.rb' emits warnings with '-w'. That probably isn't my fault.

== The C extension

* We should try to locate methods defined by define_method() (NODE_BMETHOD).
  E.g., used in the 'Colored' gem and in Sinatra::Base.
